apiVersion: v1
kind: Service
metadata:
  labels:
    app: blackbox-exporter
    addonmanager.kubernetes.io/mode: Reconcile
  name: blackbox-exporter
  namespace: <%= @namespace %>
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: <%= @port %>
  selector:
    app: blackbox-exporter
