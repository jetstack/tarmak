apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
    addonmanager.kubernetes.io/mode: Reconcile
  name: prometheus
  namespace: <%= @namespace %>
spec:
  ports:
  - port: <%= @port %>
    protocol: TCP
    targetPort: <%= @port %>
  selector:
    app: prometheus
